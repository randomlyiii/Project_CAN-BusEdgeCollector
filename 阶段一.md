# 阶段一：STM32 CAN通信 + 主站上位机交互 + OLED功能验证

## 阶段目标

完成主站（CanEdgeGateWay_Master）与从站（CanEdgeGateWay_Slave）之间的CAN总线基础通信，实现主站通过W5500以太网模块与上位机（PC）的双向数据交互，并通过OLED显示屏在两端实时显示通信状态和数据内容，完成闭环功能验证。

---

## 一、硬件平台

| 模块 | 主站 (Master) | 从站 (Slave) |
|---|---|---|
| MCU | STM32F103C8T6 | STM32F103C8T6 |
| CAN收发器 | ISO1050 / TJA1050 | ISO1050 / TJA1050 |
| 以太网 | W5500 (SPI) | — |
| 显示 | 0.96寸 OLED (I2C, 0x78, PB8/PB9) | 0.96寸 OLED (I2C, 0x78, PB8/PB9) |
| 传感器 | — | DHT11 温湿度 (PB11) |
| 按键 | — | KEY1=PA0 (报警), KEY2=PA1 (恢复) |
| 调试串口 | USART1 (PA9/PA10) | USART1 (PA9/PA10) |

---

## 二、任务分解

### 任务1：STM32主站与从站CAN通信

#### 1.1 CAN硬件初始化

- **CAN引脚配置**（主站/从站相同）：
  - CAN1_TX → PA12
  - CAN1_RX → PA11
- **CAN外设参数**：
  - 波特率：500 kbps（APB1=36MHz, Prescaler=5）
  - 模式：正常模式（Normal Mode）
  - 帧格式：标准帧（StdId）
  - 过滤器：32位掩码模式，全通过（Mask=0）

#### 1.2 CAN应用层协议定义（阶段一简化版）

```
帧ID分配（11位标准帧）：
  ID 0x100 ~ 0x10F : 0级紧急任务帧（急停/报警/故障上报）
  ID 0x200 ~ 0x20F : 1级常态任务帧（温湿度数据/心跳报文）
  ID 0x300 ~ 0x30F : 2级低频任务帧（光敏数据/参数读取）

数据帧格式（8字节）：
  Byte[0]   : 报文类型（0=紧急, 1=常态, 2=低频）
  Byte[1]   : 源节点ID（从站编号 0x01~0xFE）
  Byte[2]   : 功能码（0x01=心跳, 0x02=温湿度, 0x03=故障上报, 0x04=故障恢复, ...）
  Byte[3..6]: 数据载荷（4字节，如温湿度值、故障码）
  Byte[7]   : 校验和（暂时未启用，计算逻辑已移除）
```

#### 1.3 通信流程

**从站→主站（数据上报）**：
```
1. 从站DHT11采集温湿度 → 封装为1级常态帧
2. 从站每500ms发送心跳帧（ID 0x200 + 节点ID）
3. KEY1按下 → 立即发送0级紧急故障帧（ID 0x100 + 节点ID）
4. KEY2按下 → 发送故障恢复帧（ID 0x200 + 节点ID，功能码0x04）
```

**主站→从站（指令下发）**：
```
1. 主站接收上位机查询指令 → 转换为CAN帧 → 发送至目标从站
2. 主站周期轮询从站（替代心跳检测的主动确认）
```

#### 1.4 CAN错误处理

- 实时统计TEC/REC（CAN_ESR寄存器），映射到 Modbus 0x0011/0x0012
- 错误等级（error_level）：0=正常, 1=TEC/REC>96, 2=TEC/REC>127, 3=Bus-Off
- Bus-Off（error_level=3）：自动调用 CAN_ResetBus() 复位 CAN 外设
- OLED 第1行 CAN 字段：OK(green) / ERR(red)
- 总线负载：500kbps 最大约 4500fps，0~10000 表示 0.00%~100.00%

---

### 任务2：主站与电脑（上位机）交互

#### 2.1 W5500以太网驱动

- **SPI引脚配置**：
  - SPI1_SCK → PA5
  - SPI1_MISO → PA6
  - SPI1_MOSI → PA7
  - W5500_SCS → PA4（片选）
  - W5500_RST → PC15（复位）
  - 所有 SPI 引脚 (PA5/PA6/PA7) 配置为 AF_PP 模式
- **SPI 速率**：36MHz (Prescaler_2)
- **W5500 供电**：模块有两个 GND 引脚，都必须接地；3.3V 必须外接独立供电，不要从 STM32 3.3V 排针取电（模块 LDO 压差不够会导致工作不稳定）
- **网络参数**（固定IP）：
  - 主站IP：192.168.1.200
  - 子网掩码：255.255.255.0
  - 网关：192.168.1.1
  - 端口：502（Modbus TCP默认端口）

#### 2.2 Modbus TCP协议转换（阶段一简化版）

**支持的Modbus功能码**：
| 功能码 | 功能 | 方向 |
|---|---|---|
| 0x03 | 读取保持寄存器（读温湿度/状态） | PC → 主站 → CAN → 从站 |
| 0x06 | 写单个寄存器（写配置/控制指令） | PC → 主站 → CAN → 从站 |

**寄存器映射**（实际）：
```
寄存器地址    内容                    来源              说明
0x0000       从站1 温度（×10）       从站DHT11         253 = 25.3°C
0x0001       从站1 湿度（×10）       从站DHT11         621 = 62.1%
0x0002       从站1 心跳计数          从站              累计心跳帧数
0x0003       从站1 故障状态          从站              0=正常, 1=报警
0x0004       从站1 在线状态          主站              0=离线, 1=在线
0x0010       CAN总线负载（×100）     主站              6 = 0.06%, 10000 = 100.00%
0x0011       CAN TEC 错误计数        主站
0x0012       CAN REC 错误计数        主站
0x0013       CAN 错误等级            主站              0正常/1主动/2被动/3总线关闭
```

#### 2.3 上位机验证工具

- **Modbus Poll**（Windows）：模拟Modbus TCP主站，读取主站寄存器
- **串口助手**：通过USART1查看主站调试日志
- **网络调试助手**：验证W5500 TCP连接状态

#### 2.4 通信流程

```
1. PC(Modbus Poll) --[Modbus TCP]--> W5500 --[SPI]--> STM32主站
2. STM32主站解析Modbus请求 → 判断目标寄存器 → 封装CAN帧
3. CAN帧 → 目标从站 → 从站处理 → 返回数据
4. 主站接收CAN返回帧 → 更新寄存器 → 封装Modbus TCP响应 → PC
```

---

### 任务3：OLED显示屏功能验证

#### 3.1 主站OLED显示内容（4行 × 16字符）

```
Line 1: "CAN:OK  ETH:CON "     // CAN状态(OK/ERR) + 以太网(OK/FAIL/NOLK/CON)
Line 2: "S1:25.3C L:0.06%"    // 从站1 温度 + CAN总线负载(自适应精度)
Line 3: "H:62.1%  HB:12345"    // 湿度 + 心跳累计计数
Line 4: "R1234  T45   OK "     // CAN帧总数 + 温湿度帧数 + 状态(OK/ALM/BLK/OFF)
```

> L 负载显示自适应：<10% 两位小数 (0.06%)，≥10% 四舍五入一位小数 (90.7%)，100% 显示整数。

#### 3.2 从站OLED显示内容（4行 × 16字符）

```
Line 1: "S:Node#01 Ready"      // 从站节点号 + DHT11状态
Line 2: "Tmp:25.3 C      "     // 当前温度
Line 3: "Hum:62.1 %RH    "     // 当前湿度
Line 4: "TX:00036 K:1     "    // CAN发送总数 + 按键事件计数
```

#### 3.3 OLED验证清单

| 验证项 | 预期现象 | 实际 |
|---|---|---|
| 上电初始化 | 依次显示 Init/CAN OK/ETH OK/Server OK → 1.5s后进入主界面 | ✅ |
| CAN通信正常 | 主站第1行 "CAN:OK", 从站 TX 计数递增 | ✅ |
| 温湿度数据 | 主站第2/3行实时刷新温湿度, 从站同步显示 | ✅ |
| 断线检测 | 拔掉从站CAN线 → 从站温度显示 "--.-C", 第4行 "OFF" | ✅ |
| 故障上报 | 按下从站KEY1 → 主站第4行显示 "ALM" | ✅ |
| 以太网连接 | 网线插入 → 主站第1行 "ETH:CON" | ✅ |
| Modbus通信 | PC端ModbusPoll读到寄存器数据, 与OLED一致 | ✅ |

---

## 三、软件架构

### 3.1 主站程序流程

```
main()
 ├─ 系统时钟初始化 (72MHz)
 ├─ Delay_init()
 ├─ USART1_init(115200)          // 调试串口
 ├─ OLED_Init()                   // I2C OLED
 ├─ CAN1_Init(500kbps)           // CAN外设
 ├─ W5500_Init()                  // SPI + 网络配置
 ├─ ModbusTCP_Init()              // Modbus协议栈
 │
 └─ while(1) 主循环 (200ms节拍)
     ├─ CAN_HeartBeatCheck()      // 心跳超时检测(1.5s) + 黑名单管理
     ├─ CAN_ErrorMonitor()         // 错误等级监控, BUS-OFF自动复位
     ├─ CAN_CalcBusLoad()          // 总线负载率(基于窗口帧数归一化)
     ├─ W5500_TCPServer_Run()     // TCP Server 轮询
     ├─ ModbusTCP_SyncFromCAN()   // CAN数据 → Modbus寄存器(100ms)
     ├─ ModbusTCP_Process()       // 处理PC Modbus请求
     └─ OLED_Update()             // 每300ms刷新显示
     (CAN帧接收在 ISR 中直接处理, 不经过主循环)
```

### 3.2 从站程序流程

```
main()
 ├─ 系统时钟初始化 (72MHz)
 ├─ Delay_init()
 ├─ USART1_init(115200)          // 调试串口
 ├─ OLED_Init()                   // I2C OLED
 ├─ CAN1_Init(500kbps)           // CAN外设
 ├─ DHT11_GPIO_Init()            // 温湿度传感器
 ├─ KEY_Init()                   // KEY1(PA0), KEY2(PA1)
 │
 └─ while(1) 主循环
     ├─ DHT11_Read()             // 每2s采集, 结果缓存+发送温湿度帧
     ├─ CAN_SendHeartBeat()      // 每500ms发送心跳 + 同时发送温湿度
     ├─ KEY_Scan()               // 200ms冷却去抖
     │   ├─ KEY1 → CAN_SendAlarm()      // 发送紧急帧(ID 0x100)
     │   └─ KEY2 → CAN_SendRecover()    // 发送恢复帧
     └─ OLED_Update()             // 每300ms刷新显示
```

---

## 四、阶段一验收结果

| 序号 | 验收项 | 标准 | 结果 |
|---|---|---|---|
| 1 | CAN物理层连通 | 主站/从站 CAN 正常收发 | ✅ 500kbps, Prescaler=5 |
| 2 | 心跳机制 | 从站每500ms发送，主站1.5s超时 | ✅ HB 计数正常递增 |
| 3 | 温湿度上报 | OLED + ModbusPoll 可读 DHT11 数据 | ✅ 温度253=25.3°C |
| 4 | 紧急报警 | KEY1 按下，主站 OLED 显示 ALM | ✅ 黑名单拦截已修复 |
| 5 | Modbus TCP | ModbusPoll 192.168.1.200:502 读写 | ✅ 03/06 功能码正常 |
| 6 | OLED 显示 | 主站/从站 4×16 布局完整 | ✅ 全部数据可见 |
| 7 | 以太网自动恢复 | 网线插拔后 W5500 自动重连 | ✅ |
| 8 | 总线负载显示 | 低流量显示 0.XX%，自适应精度 | ✅ 0.00%~100.00% |

### 验收数据（实测）
- CAN: 主站收心跳+T/H帧正常, bus load < 5%
- OLED: 主站显示 CAN OK + ETH CON/LSN
- W5500: Modbus Poll 连接 192.168.1.200:502 读温湿度正确
- 从站: DHT11 温湿度 + OLED 显示正常

---

## 五、参考硬件接线

详见 `./排线设计/阶段一排线设计.md`

### 5.1 GND 共地原则 & 供电注意事项（已踩坑确认）

| 场景 | 要求 | 后果（实测） |
|------|------|------|
| 主站 ↔ 从站 CAN | **必须连 GND** | 不共地 → TJA1050 共模电压超范围 → 数据全错 |
| W5500 ↔ STM32 | **双 GND 都接地** | 只接一个 → SPI 时断时续 |
| ST-Link ↔ 蓝 Pill | **只接 SWDIO/SWCLK/GND** | 接 3.3V → 电源对顶、电脑闪屏 |
| 外接电源 | **所有 GND 汇到一起** | 多点供电不共地 → 地回路电流 |
| **W5500 供电** | **外接独立 3.3V** | 从 STM32 取电 → LDO 欠压 → 模块工作不稳定 |
| **BOOT0 跳线** | **必须拨到 0** | BOOT0=1 → 程序不跑 |
| **主供电** | **USB 供电即可** | 劣质外接 5V 电源可能嗡嗡响/烫手 |

---

## 六、参考源码

- CAN驱动库：`CanEdgeGateWay_Master/Library/stm32f10x_can.c`
- OLED驱动：`CanEdgeGateWay_Master/Hardware/OLED.c`
- DHT11驱动：`CanEdgeGateWay_Slave/Hardware/dht11.c`
- 硬件厂家CAN示例：`./硬件厂家资料/can程序源码/`
