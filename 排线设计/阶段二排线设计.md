# 阶段二排线设计

> 硬件变更：从站新增**光敏电阻模块（LM393比较器，DO/AO输出）**，使用 PB0(DO) + PB1(AO/ADC1_IN9)。主站硬件与阶段一完全相同。
> 驱动文件：`lm393.c` / `lm393.h`（旧 BH1750 I2C 代码已改名 `bh150.c/h` 备查）

---

## 一、变更摘要

| 模块 | 阶段一 | 阶段二 | 说明 |
|------|--------|--------|------|
| 主站 | STM32 + OLED + W5500 + TJA1050 | **不变** | 无硬件改动 |
| 从站 | STM32 + OLED + DHT11 + TJA1050 + KEY×2 | **+ 光敏电阻模块** | 新增 DO/AO 光敏模块，独立引脚 |
| CAN 总线 | 主↔从 CAN_H / CAN_L / GND | **不变** | 无新增节点 |
| 供电 | 外接 5V/3.3V 或 USB | **不变** | 光敏模块功耗极低，不影响预算 |

---

## 二、系统整体接线拓扑

```
                    PC (上位机)
              Modbus TCP / ST-Link
                        │
                   ┌────┴────┐
                   │ 以太网   │ ST-Link
                   │ (RJ45)   │ (SWD)
                   │          │
┌──────────────────┴──────────┴───────────────┐
│             主站 (面包板 #1)                  │
│  ┌───────────────────────────────────────┐   │
│  │ STM32F103C8T6        W5500 以太网     │   │
│  │ OLED(0x78)           TJA1050 CAN      │   │
│  │ [SWD 4P排针]                          │   │
│  └───────────────────────────────────────┘   │
└──────────────────────┬──────────────────────┘
                        │ CAN_H / CAN_L / GND (杜邦线)
┌───────────────────────┴──────────────────────┐
│             从站 (面包板 #2)                   │
│  ┌───────────────────────────────────────┐   │
│  │ STM32F103C8T6                         │   │
│  │ OLED(0x78)   光敏模块(DO/AO) ← 新增   │   │
│  │ DHT11  KEY1 KEY2  TJA1050 CAN         │   │
│  │ [SWD 4P排针]                          │   │
│  └───────────────────────────────────────┘   │
└──────────────────────────────────────────────┘
```

---

## 三、主站 (Master) 接线

> **与阶段一完全一致，无任何变更。**

### 3.1 引脚分配表

| STM32引脚 | 连接目标 | 说明 |
|---|---|---|
| PC15 | W5500 RST | 以太网复位 |
| PA4 | W5500 SCS | SPI 片选 |
| PA5 | W5500 SCLK | SPI 时钟 |
| PA6 | W5500 MISO | SPI 主入从出 |
| PA7 | W5500 MOSI | SPI 主出从入 |
| PA11 | TJA1050 RXD | CAN RX |
| PA12 | TJA1050 TXD | CAN TX |
| PB8 | OLED SCL | 软件 I2C 时钟 |
| PB9 | OLED SDA | 软件 I2C 数据 |

### 3.2 OLED（同阶段一）

```
OLED (0.96寸, I2C, 4pin)
  GND → GND
  VCC → 3.3V
  SCL → PB8
  SDA → PB9
```

### 3.3 TJA1050 CAN 收发器（同阶段一）

```
TJA1050 模块
  VCC  → 5V      (板子5V排针或外接电源)
  GND  → GND
  TXD  → PA12
  RXD  → PA11
  CANH → CAN_H 总线 (杜邦线→从站)
  CANL → CAN_L 总线 (杜邦线→从站)
```

### 3.4 W5500 以太网模块（同阶段一）

```
W5500 模块
  GND  → GND   ← 模块有两个GND引脚，都要接地！
  GND  → GND   ← (第2个GND)
  3.3V → 3.3V  ← 外接独立3.3V，不要从STM32 3.3V排针取电
  SCLK → PA5
  MISO → PA6
  MOSI → PA7
  SCS  → PA4
  RST  → PC15
  INT  → 不接 (轮询模式)
  RJ45 → 网线 → PC
```

### 3.5 ST-Link（同阶段一）

```
ST-Link           蓝 Pill SWD 排针
  SWDIO  →    SWDIO
  SWCLK  →    SWCLK
  GND    →    GND
  3.3V   →    不接！(板子USB供电，接3.3V会导致电源对顶)
```

---

## 四、从站 (Slave) 接线（★ 光敏模块新增）

### 4.1 引脚分配表

| STM32引脚 | 连接目标 | 功能 | 变更 |
|---|---|---|---|
| PA0 | KEY1（故障上报） | GPIO 输入，内部上拉 | — |
| PA1 | KEY2（故障恢复） | GPIO 输入，内部上拉 | — |
| PA11 | TJA1050 RXD | CAN RX | — |
| PA12 | TJA1050 TXD | CAN TX | — |
| PB0 | **光敏模块 DO** | GPIO 输入（数字阈值） | ★ 新增 |
| PB1 | **光敏模块 AO** | ADC1_IN9（模拟光照值） | ★ 新增 |
| PB8 | OLED SCL | 软件 I2C 时钟 | — |
| PB9 | OLED SDA | 软件 I2C 数据 | — |
| PB11 | DHT11 DATA | 单总线 | — |

### 4.2 光敏电阻模块（LM393）

常见 4pin 光敏电阻模块，采用 **LM393 双路比较器**，板载光敏电阻（GL55系列）+ 可调电位器：

```
光敏模块 (4pin)
  VCC → 3.3V
  GND → GND
  DO  → PB0   ← 数字输出（超过阈值=LOW, 低于=HIGH，电位器可调）
  AO  → PB1   ← 模拟输出（0~3.3V，光越强电压越高）
```

#### 模块工作原理

```
         VCC (3.3V)
           │
          ┌┴┐
          │ │  光敏电阻 (GL55: 亮=5~10kΩ, 暗=0.5~1MΩ)
          │ │
          └┬┘
           ├──→ AO (分压点，光越强→阻值越小→电压越高)
           │
          ┌┴┐
          │ │  固定电阻 (10kΩ)
          │ │
          └┬┘
           │
          GND

  AO ──→ LM393 同相输入(+)
         反相输入(-) ← 电位器分压 (阈值电压)
         LM393 输出 → DO (开漏，需上拉)
```

- **光强 ↑** → 光敏电阻值 ↓ → AO 电压 ↑ → ADC 读值 ↑
- **AO 电压 > 阈值** → DO = LOW (亮灯)，反之 DO = HIGH (暗灯)
- DO 为 **开漏输出**，PB0 配置为 **内部上拉（IPU）**，无需外接电阻

#### 引脚选择说明

| 引脚 | 选型理由 |
|------|---------|
| PB0 (DO) | GPIO 数字输入（IPU），用于光照阈值告警；可选配 EXTI 中断 |
| PB1 (AO) | ADC1_IN9 通道，12 位 ADC 读取光照强度（0~4095）；与 DO 同属 GPIOB 端口，接线方便 |

#### 电位器调节

| 方向 | 效果 |
|------|------|
| 顺时针拧 | 阈值电压升高 → DO 更难触发 LOW → 需要更大光强 |
| 逆时针拧 | 阈值电压降低 → DO 更容易触发 LOW → 较小光强即触发 |

> 调试建议：在正常室内光照下，缓慢逆时针拧电位器直到 DO LED 刚好熄灭，此时为临界阈值。再逆时针拧 1/8 圈，确保正常光照下 DO=LOW（亮）。

#### 驱动 API（lm393.c / lm393.h）

```c
void     Lm393_Init(void);        // 初始化 PB0(IPU) + PB1(AIN) + ADC1
uint8_t  Lm393_ReadDigital(void); // 读 DO → 0=亮, 1=暗
uint16_t Lm393_ReadAnalog(void);  // 读 AO → ADC 原始值 0~4095
```

#### ADC → OLED 显示映射

```
ADC 原始值 (0~4095)    OLED 显示
     0         →       0%   (全暗)
  2048         →      50%   (半亮)
  4095         →     100%   (全亮)

公式: light_pct = (ADC * 100) / 4095
OLED 格式: "L:xxx%" (3位右对齐, 16字符行内)
```

#### 与 OLED 的关系

> 光敏模块使用独立的 PB0/PB1，**不占用** OLED 的 PB8/PB9 I2C 总线。两个设备互不影响，无需总线仲裁。

### 4.3 DHT11 温湿度（同阶段一）

```
DHT11 (3pin)
  VCC  → 3.3V
  DATA → PB11 (需 4.7kΩ 上拉，模块已内置则免)
  GND  → GND
```

### 4.4 按键（同阶段一）

```
KEY1: 一端→PA0, 另一端→GND
KEY2: 一端→PA1, 另一端→GND
(PA0/PA1 内部上拉，按下=低电平)
```

### 4.5 TJA1050 CAN 收发器（同阶段一）

```
TJA1050 模块
  VCC  → 5V
  GND  → GND
  TXD  → PA12
  RXD  → PA11
  CANH → CAN_H 总线
  CANL → CAN_L 总线
```

### 4.6 ST-Link（同阶段一）

```
ST-Link           蓝 Pill SWD 排针
  SWDIO  →    SWDIO
  SWCLK  →    SWCLK
  GND    →    GND
  3.3V   →    不接！
```

---

## 四（附录）、驱动文件变更对照

| 文件名 | 状态 | 说明 |
|--------|------|------|
| `Hardware/bh1750.c` | ❌ 已删除 | 旧 BH1750 I2C 驱动，与实物不匹配 |
| `Hardware/bh1750.h` | ❌ 已删除 | 同上 |
| `Hardware/bh150.c` | 📦 保留备查 | bh1750.c 改名，不参与编译 |
| `Hardware/bh150.h` | 📦 保留备查 | bh1750.h 改名，不参与编译 |
| **`Hardware/lm393.c`** | ✅ **编译使用** | LM393 光敏模块驱动（PB0/PB1） |
| **`Hardware/lm393.h`** | ✅ **编译使用** | 头文件 |

> `bh150.c/h` 不在 `.uvprojx` 工程中，仅供将来需要 I2C 方案时参考。当前编译队列为 `lm393.c`。

---

## 五、CAN 总线（同阶段一）

```
主站 CAN_H ──── 杜邦线 ──── 从站 CAN_H
主站 CAN_L ──── 杜邦线 ──── 从站 CAN_L
主站 GND   ──── 杜邦线 ──── 从站 GND   (共地必须！)
```

- 桌面短距离（<30cm）：不接终端电阻
- 长距离：需 120Ω 1/4W 终端电阻
- CAN 差分总线也必须共地（TJA1050 共模电压以本地 GND 为参考）

---

## 六、供电（同阶段一）

```
外接 5V/3.3V 电源
  5V  → 主站 5V 排针  → TJA1050 VCC
       → 从站 5V 排针  → TJA1050 VCC
  3.3V → 主站 3.3V 排针 → OLED / W5500
       → 从站 3.3V 排针 → OLED / DHT11 / 光敏模块
  GND  → 主站 GND / 从站 GND
```

或每块蓝 Pill 单独 USB 供电（桌面调试推荐）。

---

## 七、面包板布局

```
面包板#1 (主站)                   面包板#2 (从站)
┌──────────────────────┐        ┌──────────────────────────┐
│ STM32       W5500    │        │ STM32       DHT11       │
│ OLED        TJA1050  │        │ OLED        光敏模块 ←新 │
│ [SWD排针]            │        │ KEY1/2      TJA1050      │
│                      │        │ [SWD排针]                │
└──────────────────────┘        └──────────────────────────┘

面包板#3 (备用)
```

---

## 八、从站引脚总览

```
         ┌──────────────────────────┐
         │     STM32F103C8T6        │
         │                          │
  PB0 ←──┤ 光敏 DO                  │
  PB1 ←──┤ 光敏 AO (ADC1_IN9)       │
  PB8 ──→┤ OLED SCL                │
  PB9 ←─→┤ OLED SDA                │
  PB11 ←─→┤ DHT11 DATA             │
  PA0 ←──┤ KEY1                    │
  PA1 ←──┤ KEY2                    │
  PA11 ←──┤ TJA1050 RXD            │
  PA12 ──→┤ TJA1050 TXD            │
         │                          │
         │  PA9  → USART1 TX (调试) │
         │  PA10 ← USART1 RX (调试) │
         └──────────────────────────┘
```

---

## 九、检查清单

| # | 检查项 | ✓ |
|---|---|---|
| 1 | 主站 OLED：PB8→SCL, PB9→SDA, VCC→3.3V, GND→GND | ☐ |
| 2 | 主站 TJA1050：PA11→RXD, PA12→TXD, VCC→5V, GND→GND | ☐ |
| 3 | 主站 W5500：PA5→SCLK, PA6→MISO, PA7→MOSI, PA4→SCS, PC15→RST, **双GND都接地**, 外接3.3V供电 | ☐ |
| 4 | 主站 ST-Link：SWDIO/SWCLK/GND（不接3.3V） | ☐ |
| 5 | 从站 OLED：PB8→SCL, PB9→SDA, VCC→3.3V, GND→GND | ☐ |
| 6 | 从站 TJA1050：PA11→RXD, PA12→TXD, VCC→5V, GND→GND | ☐ |
| 7 | 从站 DHT11：PB11→DATA, VCC→3.3V, GND→GND | ☐ |
| 8 | 从站按键：PA0→KEY1→GND, PA1→KEY2→GND | ☐ |
| 9 | ★ 光敏模块 VCC→3.3V, GND→GND | ☐ |
| 10 | ★ 光敏模块 DO→PB0，AO→PB1 | ☐ |
| 11 | ★ 光敏模块与 OLED 独立走线，不共用 I2C 总线 | ☐ |
| 12 | ★ 电位器调试：正常光照下 DO LED 刚好熄灭 → 临界阈值 | ☐ |
| 13 | ★ OLED 显示验证：手遮光敏电阻 → L%降；手电筒照射 → L%升 | ☐ |
| 14 | CAN 总线：CAN_H 互连, CAN_L 互连, GND 互连 | ☐ |
| 15 | 供电：5V 给 TJA1050, 3.3V 给 OLED/W5500/DHT11/光敏模块 | ☐ |
| 16 | 网线：主站 W5500 RJ45 → PC | ☐ |

> ★ = 阶段二新增检查项
