# CAN Bus Edge Collector — 学习参考文档

> **文档说明**：本文档为学习交流演示版本。项目基础业务架构完整可供嵌入式学习参考；部分工业场景深度容错、精细化总线防护的核心实现方案做了简化脱敏处理，作为进阶拓展方向，未完整公开。仅限个人技术练习，请勿直接商用。

---

## 项目概述

一套基于 CAN 总线的传感器数据采集与转发系统。主站（STM32F103 + W5500）作为 Modbus TCP 网关，将从站采集的温度、湿度、光照等传感器数据转发给上位机；从站（STM32F103）执行传感器数据采集与故障上报。

### 适用场景

产线传感器采集、环境监测、简单的分布式数据采集教学演示。

### 硬件组成

| 器件 | 型号 | 说明 |
|------|------|------|
| 主控 MCU | STM32F103C8T6 | 72MHz Cortex-M3 |
| CAN 收发器 | TJA1050 | CAN 总线接口 |
| 以太网芯片 | W5500 | SPI 接口硬件 TCP/IP |
| 显示屏 | 0.96" SSD1306 OLED | 128×64，I2C |
| 传感器 | DHT11 / BH1750 / LM393 | 温湿度 / 光照 / 开关量 |

CAN 波特率：500kbps。

---

## 目录结构

```
|--README.md
|--CanEdgeGateWay_Master/          主站（以太网网关）
|--CanEdgeGateWay_Slave/           从站（传感器采集）
|--phase_md/                       设计文档与调试记录
|--排线设计/                        引脚排线定义
```

主从站各为独立 Keil MDK 工程，可分别编译烧录。

---

## 系统架构

### 业务流程

```
传感器 → 从站采集 → CAN 总线 → 主站接收 → Modbus TCP → 上位机
```

1. 从站定时采集传感器数据，组包为 CAN 帧发送到总线
2. 主站通过 CAN 控制器接收所有帧，解析后更新内部数据表
3. 上位机通过 Modbus TCP 协议读取主站寄存器获取实时数据
4. 从站检测到传感器故障时主动上报报警帧，恢复正常后上报恢复帧

### 主站软件架构

三任务协同架构：

| 任务 | 优先级 | 周期 | 职责 |
|------|--------|------|------|
| CAN_Rx | 高 | 10ms | CAN 帧接收与解析（从软件环形缓冲取出处理） |
| Unified | 普通 | 10ms | 心跳/错误/负载监控、以太网 W5500、Modbus、按键 |
| Housekeep | 低 | 1s | OLED 页面切换刷新 |

```
CAN 接收链:
  硬件 FIFO → 中断搬入软件环形缓冲 → CAN_Rx 任务取出解析
```

**关键设计**：CAN 帧处理独立成高优先级任务。即使以太网（W5500）长时间占用主任务，CAN 帧仍由独立任务及时消费——实测总线满速（约 4100fps）灌入零丢帧，处理能力远超总线物理上限。

关键约束：SPI（W5500）与 CAN RX 共用 GPIOA 端口，硬件上不可并发访问，以太网操作与 CAN 监控在主任务内顺序执行。

### 从站软件架构

多任务协同：

| 任务 | 优先级 | 周期 | 职责 |
|------|--------|------|------|
| Sensor | 最高 | 200ms | 定时采集各传感器数据 |
| CAN | 较高 | 500ms | 发送心跳帧与传感器数据 |
| Key | 普通 | 20ms | 检测按键触发报警/恢复 |
| Housekeep | 最低 | 1s | OLED 显示、喂看门狗 |

---

## CAN 通信协议

### 帧格式（8 字节标准帧）

| Byte0 | Byte1 | Byte2 | Byte3~6 | Byte7 |
|-------|-------|-------|---------|-------|
| 优先级 | 源节点 ID | 功能码 | 载荷 | 校验和（异或）|

**接收端完整性校验**：主站收到帧后，先检查 DLC=8（数据长度），再计算 byte0~6 异或值对比 byte7。任一不通过直接丢弃。

### 帧 ID 分配

| 优先级 | ID 范围 | 用途 |
|--------|---------|------|
| 紧急帧 | 0x100 ~ 0x1FF | 报警/故障上报 |
| 常态帧 | 0x200 ~ 0x2FF | 心跳、温湿度数据 |
| 低频帧 | 0x300 ~ 0x3FF | 光照、开关量 |

### 功能码定义

| 码值 | 含义 | 数据来源 |
|------|------|---------|
| 0x01 | 心跳 | 从站周期上报 |
| 0x02 | 温湿度 | DHT11 |
| 0x03 | 报警 | 传感器故障 / 按键触发 |
| 0x04 | 恢复 | 故障排除后发送 |
| 0x05 | 光照/模拟量 | BH1750 / LM393 AO |
| 0x06 | 数字量 | LM393 DO |

---

## Modbus TCP 寄存器映射

主站提供 Modbus TCP 服务（端口 502），上位机通过功能码 0x03 读取、0x06 写入。

### 常用监控寄存器

| 地址 | 名称 | 说明 |
|------|------|------|
| 0x0000 | 从站1 温度 | 实际值 × 10 |
| 0x0001 | 从站1 湿度 | 实际值 × 10 |
| 0x0002 | 从站1 心跳计数 | |
| 0x0004 | 从站1 在线标志 | 0=离线 1=在线 |
| 0x0005~0x000B | 从站2 数据 | 同上 |
| 0x0010 | CAN 总线负载 | 0~10000（×0.01%）|
| 0x0013 | CAN 错误等级 | 0=正常 |

### 常用控制寄存器

| 地址 | 写入值 | 作用 |
|------|--------|------|
| 0x0020 | 0x55 | 复位 CAN 控制器 |

---

## 从站传感器采集

### 传感器管理框架

传感器抽象层采用表驱动设计，每种传感器注册为一个描述符，包含初始化函数、读取函数、采样间隔等：

```c
typedef struct {
    uint8_t   type_id;        // 传感器类型
    uint16_t  interval_ms;    // 采样间隔
    uint8_t  (*init_fn)(void);       // 初始化
    uint8_t  (*read_fn)(data_t*);    // 读取
    uint8_t   enabled;        // 启用标志
    uint8_t   online;         // 在线状态
    uint8_t   fault_count;    // 连续失败次数
    data_t    last_data;      // 最近一次数据
} sensor_t;
```

传感器表通过条件编译展开，未启用的传感器不编译：

```c
static sensor_t g_sensor_table[] = {
    #if HAS_TEMP_HUMI
        { .type_id = 0x01, .interval_ms = 2000, .init_fn = dht11_init, .read_fn = dht11_read, .enabled = 1 },
    #endif
    #if HAS_LIGHT
        { .type_id = 0x02, .interval_ms = 2000, .init_fn = bh1750_init, .read_fn = bh1750_read, .enabled = 1 },
    #endif
    // ...
};
```

### 传感器故障处理

传感器连续读取失败累计计数。达到阈值后标记告警状态，通过 CAN 发送报警帧。恢复正常后自动发送恢复帧。

### CAN 发送流程

每周期按以下顺序发送：

1. 心跳帧（表征节点存活）
2. 告警/恢复帧（紧急优先，不参与限流）
3. 在线传感器数据
4. 缓存补传（发送失败的帧暂存后补发）

---

## 心跳检测与节点管理

### 心跳机制

从站每 500ms 发送一次心跳帧。主站跟踪每个节点最后收到帧的时间戳：

- 超过 3s 未收到有效帧 → **过期**（EXP）
- 过期状态持续 30s → **离线**（OFF）
- 收到通过完整性校验的帧 → 自动恢复正常（ON）

**节点隔离**：每个节点独立跟踪状态。总线错误不影响节点在线判定，单节点断线不牵连其他节点正常通信。

### 节点状态

主站 OLED 显示每个节点的当前状态：

```
ON  — 正常
EXP — 短时无数据
OFF — 确认离线
ALM — 告警
```

### 告警帧限流

紧急告警帧收发均有限流，防止单节点故障引发的总线风暴：

- 从站：将告警帧发送频率控制在 1 次/秒，连续触发后降为 2 次/秒
- 主站：接收侧每节点 200ms 限频，超出丢弃并计数
- 丢弃计数可作为诊断指标，反映总线拥塞程度

---

## 功能实现一览

| 功能 | 状态 | 说明 |
|------|------|------|
| CAN 优先级分组 | 完成 | 紧急/常态/低频三档 |
| 总线负载计算 | 完成 | 100ms 窗口实时计算（按真实帧长折算）|
| CAN 高吞吐接收 | 完成 | 中断 + 环形缓冲 + 独立任务，总线满速零丢帧 |
| 心跳超时检测 | 完成 | EXP→OFF 两段判定 |
| Modbus TCP 转发 | 完成 | 实时 + 历史缓存 |
| 传感器抽象层 | 完成 | 表驱动 + 故障累计 |
| 多从站配置 | 完成 | 配置文件切换传感器组合 |
| OLED 显示 | 完成 | 多页切换显示 |
| 双 FIFO 缓冲 | 完成 | 紧急/常态分级存储 |
| 升降级机制 | 进阶拓展 | 见下方说明 |
| 多客户端支持 | 进阶拓展 | 当前仅单连接 |
| 固件升级 | 进阶拓展 | 当前需烧录器 |

---

## 进阶拓展方向

> 以下为工业级深度容错能力的核心方向概览。完整实现涉及较复杂的时序配合与状态机设计，在此仅提供思路，不展开完整实现。可作为嵌入式进阶学习的方向参考。

### 1. BusOff 精细化恢复策略

CAN 控制器在总线错误超过阈值后会自动断开总线（BusOff）。基础方案是简单的复位重试，进阶方向包括：

- 区分偶发干扰与持续故障，自适应拉长恢复间隔
- 恢复前等待网络状态稳定，避免高频无效复位
- 接入 CAN 控制器错误计数器（TEC/REC），根据计数变化趋势判断总线健康度

### 2. 主从站差异化总线防护

不同节点在系统中的角色不同，采用的防护策略应有区别：

- **主站**：网关节点，应保守处理总线错误，避免频繁复位影响数据上报
- **从站**：采集节点，应快速自行恢复，减少对总线的影响
- 数据传输错误时的重传策略：硬件自动重传 vs 应用层按需重传

### 3. 动态优先级升降级

根据节点运行状态动态调整其数据优先级：

- 传感器数据超限 → 临时提升为紧急帧
- 总线负载过高 → 限制非紧急帧的发送频率
- 故障恢复后 → 恢复原始优先级
- 进阶方案可加入超时自动降级、手动干预降级等

---

## 免责声明

本文档仅供个人技术学习与交流。作者不对以下情况承担责任：

1. 直接套用本文档设计用于商业产品或工业现场
2. 因省略工业级容错实现而导致的数据丢失或设备损坏
3. 未经充分测试即部署到生产环境

工业现场应用请咨询专业人员，增加必要的安全防护与冗余设计。

---

## 调试经验

### 快速定位 CAN 通信问题

当主站收不到从站帧时，按以下顺序排查：

1. **读硬件 FIFO 状态**：检查 `CAN1->RF0R & 3`（FMP0 待处理帧数）。>0 说明帧到了 CAN 控制器级别，是软件处理问题；=0 说明收发器或总线物理层问题
2. **检查连接**：TJA1050 模块排针松动会导致偶发断联。多节点正常不一定说明硬件完好——其他节点的机械振动可能提供临时接触
3. **用计数器分段定位**：在 CAN_Receive 调用处放计数器，对比处理完成计数器，差值即丢失位置

### 常见误区

- "中断和轮询可以同时用" → 不可。ISR 和 Task 读同一硬件 FIFO 产生竞态，帧被 ISR 抢走后 Task 永远找不到
- "多节点正常说明硬件没问题" → 不一定。其他节点的物理连接可能掩盖了本节点的接触故障
- "采样点 75% 比 50% 好" → 取决于总线拓扑和节点数。单节点短距离 50% 更稳定，多节点长距离 75% 更可靠
- "中断处理函数名随便写" → 不可。F103 的 CAN RX0 中断向量名是 `USB_LP_CAN1_RX0_IRQHandler`，不是 `CAN1_RX0_IRQHandler`。名字写错，linker 会把函数当未用符号删除，中断触发后跳进启动文件的默认死循环，表现为程序卡死

---

## 参考资源

- STM32F103 参考手册（RM0008）
- W5500 数据手册
- Modbus TCP 协议规范（IEC 61158）
- CAN 总线协议规范（ISO 11898）
- FreeRTOS 官方文档
