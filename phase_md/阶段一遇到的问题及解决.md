# 阶段一遇到的问题及解决

## 一、OLED

| # | 问题 | 原因 | 解决 |
|---|---|---|---|
| 1 | 闪一下就熄灭 | `Delay_us()` 把 SysTick CTRL 写死 0x04，中断停了 | 保存/恢复 CTRL |
| 2 | 软件 I2C 太快 OLED 不响应 | 72MHz 下 GPIO 翻转 > 400kHz | I2C Start/Stop/SendByte 跳变间加 ~2μs 延时 |
| 3 | 推挽模式导致总线冲突 | ACK 位主从同时驱动 SDA | 改回开漏+模块自带的上拉 |
| 4 | OLED 模块损坏 | 推挽冲突期间可能烧了 I2C 口 | 换个新 OLED |

## 二、编译/运行时

| # | 问题 | 解决 |
|---|---|---|
| 5 | `SPI_BaudRatePrescaler_9` 不存在 | 改 `_8` |
| 6 | `CAN_ESR_BOF` 未定义 | 直读 `CAN1->ESR & 0x04` |
| 7 | sprintf 半主机崩溃 | 加 `#pragma import(__use_no_semihosting_swi)` |

## 三、DHT11

| # | 问题 | 原因 | 解决 |
|---|---|---|---|
| 8 | 显示一次后卡死 | OLED_UpdateDisplay 和主循环两处读传感器，间隔 < 1s 传感器不应答 → while 永久卡死 | OLED 读缓存，主循环 2s 只读一次 |
| 9 | 超时太短读不到数据 | 255 次循环 ≈ 12μs < DHT11 应答 80μs | 加大超时到 5000+ |
| 10 | 内部上拉导致读失败 | 模块已自带外部上拉 | 恢复 GPIO_Mode_IN_FLOATING |

## 四、CAN

| # | 问题 | 原因 | 解决 |
|---|---|---|---|
| 11 | 主站 TEC/REC=0, RX=0 | CANH/CANL 没互通 + 波特率 600k | 连 CANH-CANH, CANL-CANL, GND-GND；Prescaler 4→5(500kbps) |
| 12 | RX 在涨但帧校验全丢弃 | `CAN_CalcChecksum` 函数调用约不到字节 | 临时去掉校验 |
| 13 | HB 偶尔 +1, TH 涨正常 | 心跳+温湿度两帧背靠背, ISR 单缓冲覆盖 | ISR 内 while 循环处理 FIFO 全部帧, 每帧用栈局部变量 |

## 五、按键

| # | 问题 | 原因 | 解决 |
|---|---|---|---|
| 14 | 怎么按都没反应 | GPIO_Read 后 `?0:1` 反转了极性 | 去掉反转, 改用简单下降沿+冷却逻辑 |

## 六、供电/硬件

| # | 问题 | 解决 |
|---|---|---|
| 15 | 外接 5V 电源嗡嗡响/烫手 | 扔掉，改用 USB 供电 |
| 16 | ST-Link 接 3.3V 导致电源对顶、电脑闪屏 | ST-Link 只接 SWDIO/SWCLK/GND 三根 |
| 17 | BOOT0 跳线在 1 导致程序不跑 | 拨到 0 |
| 18 | **W5500 双 GND 只接了一个** — SPI 通信时断时续 | W5500 模块有两个 GND 引脚（逻辑地 + 功率地），必须都接到 STM32 的 GND，只接一个会导致 SPI 波形畸变或 W5500 间歇性复位 |
| 19 | **CAN 主从站不共地** — CAN 收发器共模电压超范围 | CAN_H/CAN_L 差分信号仍需公共地参考，主从站 GND 之间必须拉一根杜邦线连通 |

## 七、W5500 以太网（已修复 — 2026-07-24）

### 现象
OLED 显示 `IP:0.0.0.0` + `E:FAIL`，Modbus 无法连接。

### 根因

| # | 问题 | 原因 | 解决 |
|---|------|------|------|
| 20 | **RST 引脚错误** | 配成 PA3，实际 W5500 复位脚是 **PC15** | 参考 Project_template 修正 |
| 21 | **MISO 模式错误** | 配成 `IN_FLOATING`，模板需 `AF_PP` | PA5/PA6/PA7 全部 `GPIO_Mode_AF_PP` |
| 22 | **SPI 帧格式不匹配** | 地址嵌入 BSB → 芯片不识别 | 参考模板: `[AddrH][AddrL][Ctrl=FDM1\|R/W\|BSB][数据]` |

### 最终引脚

| 引脚 | 端口 | 功能 |
|------|------|------|
| RST | PC15 | 复位 |
| SCS | PA4 | 片选 |
| SCK/MISO/MOSI | PA5/PA6/PA7 | SPI |
| SPI 速率 | 36MHz (Prescaler_2) | |

### W5500 供电问题

| # | 问题 | 原因 | 解决 |
|---|---|---|---|
| 23 | **W5500 模块不工作/不稳定** | 模块走 5V→3.3V 板载 LDO，但某些劣质模块 LDO 压差不够（5V USB 实际 4.6~4.8V），3.3V 输出偏低导致 W5500 复位异常或 SPI 通信失败 | 外接独立 3.3V 给模块供电（不从模块 5V 引脚取电），或换用质量好的模块 |

### Claude Vibe Coding 的局限性

W5500 驱动编写是阶段一最大的难点。Claude（vibe coding 模式）**无法独立完成** W5500 驱动的原因：

1. **寄存器多且交互复杂**：W5500 有 100+ 寄存器，通过 BSB（Block Select Bits）+ 16 位地址间接访问，SPI 帧格式为 `[AddrH][AddrL][Ctrl=BSB|R/W][Data]`，一步错全盘错
2. **需要精确的硬件数据手册知识**：SPI 模式 0/3、SCLK 极性、最大 SPI 速率（理论 33.3MHz 实际受限于 PCB 走线）、PHY 初始化顺序等，AI 只能从训练数据中"猜"而非查阅手册
3. **缺乏硬件调试闭环**：AI 写出驱动 → 烧录 → 不工作，AI 无法用示波器抓 SPI 波形、无法测供电纹波、无法验证寄存器读写是否正确，只能靠枚举各种可能性碰运气
4. **最终方案**：参考 `Project_template/` 中已验证的 W5500 驱动模板，对比排查出 RST 引脚、MISO 模式、SPI 帧格式三个关键差异后修复（见 #20~#22）

**教训**：复杂外设驱动（如 W5500、以太网 PHY）应从厂家 SDK 或已验证模板开始改，不要从零手写。

## 八、Modbus TCP 数据错乱（已修复 — 2026-07-24）

### 现象
OLED 温湿度正确，Modbus Poll 读到乱值（1, 2818, 29440, -27000）。

### 根因

| # | 问题 | 原因 | 解决 |
|---|------|------|------|
| 24 | **响应数据偏移 1 字节** | `PDU_DATA_IDX=10` 是请求解析用的偏移；响应帧数据应从 byte 9 开始（MBAP(7)+FC(1)+bytecount(1)=9），不能用同一个常量 | 新增 `RESP_DATA_IDX=9`，响应构建用正确偏移 |

## 九、已完成的最终配置

| 项目 | 值 |
|---|---|
| MCU | STM32F103C8T6, HSE 8MHz, SYSCLK 72MHz |
| CAN | 500kbps, Prescaler=5, PA12(TX)/PA11(RX) |
| OLED | 0.96" I2C 0x78, PB8(SCL)/PB9(SDA), 开漏 |
| DHT11 | PB11, 2s 采集 |
| KEY | PA0(上报), PA1(恢复) |
| W5500 | RST=PC15, SCS=PA4, SPI1 36MHz, IP:192.168.1.200:502 |
| ModbusTCP | 03/06 功能码, Holding Register 0~47 |
| 供电 | USB, ST-Link 只接 SWDIO/SWCLK/GND |

### 验收数据
- CAN: 主站收心跳+T/H帧正常, bus load < 5%
- OLED: 主站显示 CAN OK + ETH LSN/CON
- W5500: Modbus Poll 连接 192.168.1.200:502 读温湿度正确
- 从站: DHT11 温湿度 + OLED 显示正确 |
